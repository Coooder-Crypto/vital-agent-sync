{
  "name": "healthlink",
  "version": "0.1.0",
  "private": true,
  "description": "Private HealthKit bridge between HealthLink iOS and a user's local Agent runtime.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev:www": "npm run dev --workspace @healthlink/www",
    "build:www": "npm run build --workspace @healthlink/www",
    "typecheck:www": "npm run typecheck --workspace @healthlink/www",
    "dev:local": "npm run dev --workspace healthlink-local",
    "build:local": "npm run build --workspace healthlink-local",
    "test:local": "npm run test --workspace healthlink-local",
    "audit:relay-local": "node scripts/e2ee-relay-local-audit.mjs",
    "audit:relay-container": "node scripts/e2ee-relay-container-audit.mjs",
    "audit:relay-package": "node scripts/e2ee-relay-package-audit.mjs",
    "audit:agent-adapters": "node scripts/agent-adapter-audit.mjs",
    "audit:dependencies": "npm audit --omit=dev --workspace healthlink-local",
    "audit:www-dependencies": "npm audit --omit=dev --workspace @healthlink/www",
    "audit:secrets": "node scripts/release-secret-scan.mjs",
    "audit:relay-hosted": "node scripts/e2ee-relay-hosted-audit.mjs",
    "preflight:relay-production": "node scripts/e2ee-relay-production-preflight.mjs",
    "release:npm-preflight": "node scripts/npm-release-preflight.mjs",
    "typecheck": "npm run typecheck --workspaces"
  },
  "engines": {
    "node": ">=22"
  },
  "overrides": {
    "postcss": "8.5.16"
  }
}
